import styled from 'styled-components'

import { FaTimes } from 'react-icons/fa'

import { CONTENT_SPACING } from 'Theme'

const PostWrapper = styled.div`
    margin: 0 0 ${CONTENT_SPACING.m} 0;
`

export { PostWrapper }
